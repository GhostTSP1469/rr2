# DebtFlow

Debt tracker app for the exam branch.

## Vercel

Deploy branch: `exam`

Environment variable:

```env
VITE_API_URL=https://debt-back-prod.onrender.com/api
```

SPA routes are handled by `vercel.json`.
